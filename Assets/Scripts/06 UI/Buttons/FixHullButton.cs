using UnityEngine;

public class FixHullButton : Button2D
{

    // protected override void OnClick()
    // {
    //     if (CombatManager.Instance.playerShipData.health >= CombatManager.Instance.playerShipData.maxHealth ||
    //         Inventory.Instance.credits < 3)
    //         return;
    //     
    //     Inventory.Instance.credits -= 3;
    //     CombatManager.Instance.playerShipData.health = CombatManager.Instance.playerShipData.maxHealth;
    // }
}
